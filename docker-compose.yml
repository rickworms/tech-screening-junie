version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: musicapi
      POSTGRES_USER: musicapi
      POSTGRES_PASSWORD: musicapi
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U musicapi -d musicapi"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  postgres_data: